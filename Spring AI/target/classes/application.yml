server:
  port: 18080
spring:
  neo4j:
    uri: bolt://127.0.0.1:7687
    authentication:
      username: neo4j
      password: your_password # 请修改为您的Neo4j密码
  ai:
    dashscope:
      api-key: sk-0f7845312d5a47e79e5b13650079b67d
    memory:
      redis:
        host: localhost
        port: 6379
        password:
        timeout: 5000
      sqlite:
        db-path: ${user.home}/.spring-ai/chat-memory.db
    chat:
      memory:
        repository:
          jdbc:
            mysql:
              jdbc-url: jdbc:mysql://localhost:3306/chatMemory?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
              username: root
              password: your_password
              driver-class-name: com.mysql.cj.jdbc.Driver
              enabled: true

# TaiKao 后端配置
taikao:
  api:
    base-url: http://localhost:8080